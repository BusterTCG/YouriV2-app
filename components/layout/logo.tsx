/**
 * Logo Youri — placeholder texte au Sprint 0. Sera remplacé par un SVG
 * customisé quand Stan aura validé un visuel (ou réutilisera l'identité
 * Pangee Prod existante).
 */
export function Logo() {
  return (
    <div className="flex items-baseline gap-1">
      <span className="text-base font-bold tracking-tight text-foreground">YOURI</span>
      <span className="text-xs font-medium text-muted-foreground">v2</span>
    </div>
  );
}
